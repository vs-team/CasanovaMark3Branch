PARSER:
- Parse lambdas
- Parse type functions
- Parse conditionals

TYPE CHECKER:
- Check the namespace when comparing types and if it is ambiguous notify it.

CODE GENERATION:
- Generate an empty result before generating the premises.
- A rule call should be executed only when the result is still empty.
- Remove the return statement at the end of the result check of each premise.
- At the end of all the possible rule calls check if the result is empty, and then return.